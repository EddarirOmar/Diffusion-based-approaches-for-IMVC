name: dcg-imvc
channels:
  - defaults
dependencies:
  - python=3.10
  - numpy
  - scipy
  - scikit-learn
  - pip
  - pip:
      - --index-url https://download.pytorch.org/whl/cpu
      - torch
      - torchvision
      - torchaudio
      - munkres
